# ToolHub Remember

- The site is a Vite React client-side app.
- Clean routes use path URLs like `/explore` and `/<tool-id>`.
- Homepage shows featured tools; `/explore` shows all tools.
- Keep tools client-side only unless explicitly requested otherwise.
- Avoid scroll jank: reserve ad dimensions, lazy/idle-load ads, and keep tool cards layout-stable.
